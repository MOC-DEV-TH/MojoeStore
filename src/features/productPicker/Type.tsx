export interface ICategoryDataType {
  id: string;
  icon: string;
  background: string;
  title: string;
}

export interface IProductDataType {
  id: string;
  name: string;
  units: number;
  price: string;
}
